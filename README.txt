######README#######

1. npm install
2. Execute node ./ping.js --host=http://localhost:8080/opencga-1.3.0-dev --sid=YOUR_TOKEN to check if everything is installed and work correctly
3. See example1.sh to execute other 2 commands.